# Core data science
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Machine learning
xgboost>=2.0

# Class imbalance
imbalanced-learn>=0.11

# Deep learning — PyTorch (binary scripts: 05_train_binary_dl.py)
# Cyber Innovation Cluster (RTX 5070 Ti, CUDA 12.8 — default pip install works):
#   pip install torch torchvision
# CPU-only:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Deep learning — TensorFlow/Keras (multiclass scripts: 07_train_multiclass_dl.py)
# Bundles its own CUDA/cuDNN — works on cluster regardless of system CUDA:
#   pip install tensorflow[and-cuda]
tensorflow>=2.16

# Visualization
matplotlib>=3.7
seaborn>=0.12
